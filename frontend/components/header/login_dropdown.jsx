import React from 'react';

class UsernameDisplay extends React.Component (currentUser, logout) {
  <div className="usernameDisplay">
    <span onClick={this.toggleVisibility}>{currentUser}</span>
    <ul>
      <li><Link User page</li>
      <li>Log Out</li>
    </ul>
  </div>
}

const UsernameDropdown = () => {
  return(
    <div>
      <ul>
        <li>User page</li>
        <li>Log Out</li>
      </ul>
    </div>
  );
};

export default UsernameDropdown;
